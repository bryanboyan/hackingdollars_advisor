/** @type {import('next-sitemap').IConfig} */
module.exports = {
  siteUrl: 'https://purplefish.com',
  generateRobotsTxt: true, // (optional)
  trailingSlash: true, // adds forward slash to all URLs
};
